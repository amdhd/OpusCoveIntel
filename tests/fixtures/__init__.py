"""Synthetic fixtures.

CLAUDE.md 7: synthetic documents only. Real prospectuses are copyrighted and
must never be committed, so the fixtures are *generated* rather than stored --
reproducible byte-for-byte, diffable as code, and free to extend with the
pathological pages (image-only scans, ruled tables) that real corpora contain
but that nobody can legally check in.
"""
